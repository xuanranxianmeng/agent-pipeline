# Reasonix 收尾规范-治本版

## 1. 为什么需要这份规范

Reasonix 经常能把活干完、把报告写到磁盘，却在最后一步报错：

- `final-answer readiness failed ... incomplete items`
- `todo_write ... no matching successful complete_step receipts`
- 最后 `exit 1`

根因很明确：**磁盘上有结果，但任务书和签收记录没有形成闭环**。  
系统要的不是“我说完成了”，而是“每一步都有对应证据、清单状态一致、最后再交付 final-answer”。

因此，收尾必须按固定三段执行，顺序不能反，不能漏，不能跳。

---

## 2. 标准收尾三段

正确顺序固定为：

1. **complete_step：把对应 todo 签完，留下证据。**
2. **todo_write：同步更新任务清单状态。**
3. **final-answer：对甲方做最终口头交付。**

### 2.1 第一段：complete_step

**白话说明：**  
这一步是在说：“这条任务我确实做完了，证据如下。”

要求：

- 每个 `todo_write` 中列出的实质任务，都必须有对应成功的 `complete_step`。
- `complete_step` 的内容要能对上 `todo` 的内容，不要张冠李戴。
- 证据可以包括：
  - `summary`：做了什么
  - `command`：如果跑过命令，写实际命令
  - `paths`：写实际涉及的文件
  - `kind`：例如 `manual`、`files`、`verification`、`review`

常见错误：

- 只写 `todo_write`，没有写 `complete_step`
- 写了 `complete_step`，但内容和 `todo` 对不上
- 多个 `todo` 只签了其中一部分

### 2.2 第二段：todo_write

**白话说明：**  
这一步是在说：“任务清单现在更新到最新状态了。”

要求：

- 必须把所有任务项一起提交，而不是只提交当前一项。
- 已完成的任务要标成 `completed`。
- 正在收尾的最后一步可以标成 `in_progress`。
- `todo_write` 的状态必须和前面已经成功的 `complete_step` 对得上。

常见错误：

- `todo_write` 里写了 3 项，但只有 2 项有 `complete_step`
- `todo_write` 仍把已签完的项标成 `in_progress` 或 `pending`
- `todo_write` 内容变了，但 `complete_step` 里签的还是旧内容

### 2.3 第三段：final-answer

**白话说明：**  
这一步是在说：“我把结果、路径、关键结论一次说清楚给甲方。”

要求：

- 只能在 `complete_step` 和 `todo_write` 都完成后才说。
- 只能说前面已经落过文件或验证过的内容。
- 不要临时口头补新结论、新数据、新文件路径。
- final-answer 是交付摘要，不是新做一轮活。

常见错误：

- 还没签收就先 final-answer
- final-answer 里引用了从未写入的报告路径
- 前面没有验证，却在 final-answer 里声称已验证

---

## 3. 任务书标准收尾段模板

下面这段可以直接粘到任何任务书的末尾，作为统一收尾约束：

```markdown
## 收尾要求

活干完后，必须按以下顺序收尾，顺序不能反、不能漏、不能跳：

1. 先对每一条已完成任务执行 `complete_step`，写清本条任务实际完成了什么，并附上证据（如文件路径、验证命令、摘要说明）。
2. 再执行一次完整的 `todo_write`，把所有任务项同步更新到最新状态；已签完的项必须标记为 `completed`。
3. 只有在前两步都完成后，才能给 `final-answer`。final-answer 只能总结已经落盘或已验证过的内容，不得新增未验证结论。

禁止事项：

- 禁止只写 `todo_write` 不写 `complete_step`。
- 禁止 `complete_step` 的签字内容与实际 `todo` 不一致。
- 禁止 `todo_write` 里列了 N 条任务，但只签了 M（M < N）条。
- 禁止在没有任何文件落盘或验证证据的情况下，直接在 final-answer 中做最终结论。

完成标准：

- 报告文件已写入磁盘且非空。
- 任务清单中所有实质任务均已完成签收。
- final-answer 仅复述已签收、已落盘、已验证的事实。
```

---

## 4. 常见踩坑场景与正确示范

### 场景一：todo 写了 3 条，但只签了 2 条

**错误表现**

- `todo_write` 里有：写报告、验证报告、收尾交付
- 实际只执行了对“写报告”和“验证报告”的 `complete_step`
- 没有对“收尾交付”做签收，就直接进入 final-answer

**为什么会报错**

因为清单和签收记录不一致，系统会判断存在未完成项。

**正确示范**

错误流程：

1. `complete_step` 签“写报告”
2. `complete_step` 签“验证报告”
3. `todo_write` 标记前两项完成，第三项仍 pending
4. 直接 `final-answer`

正确流程：

1. 每条已完成任务都分别执行一次 `complete_step`
2. 等所有对应项都签完后，再执行一次完整的 `todo_write`
3. 最后再进入 `final-answer`

正确范式：

- `complete_step`：签“写报告”
- `complete_step`：签“验证报告”
- `complete_step`：签“收尾交付”
- `todo_write`：三项全部同步更新为完成
- `final-answer`：最终交付

### 场景二：complete_step 的签字内容和 todo_write 的 checklist 对不上

**错误表现**

- `todo` 写的是“撰写收尾规范并补充踩坑场景”
- `complete_step` 却写成“修改报告格式”
- 内容语义对不上，证据链不闭合

**为什么会失败**

系统需要的是“签收对象”和“任务对象”一一对应，不是笼统地说“做过一些事”。

**正确示范**

错误范式：

- `todo`：撰写《收尾规范-治本版》并补充标准三段、模板、踩坑场景
- `complete_step`：`summary = 更新了文档结构`

问题：签的内容太虚，无法证明确实完成了对应任务。

正确范式：

- `todo`：撰写《收尾规范-治本版》并补充标准三段、模板、踩坑场景
- `complete_step`：`summary = 已写入标准三段、任务书收尾模板、3 个踩坑场景及本次任务收尾示范`

这样签收内容和任务目标一致。

### 场景三：final-answer 里引用的数据或文件，前面没有落盘就直接口头总结

**错误表现**

- 报告还没确认写入
- 报告路径还没确认存在
- 文件非空验证也没做
- 但 final-answer 里却说“报告已生成、已验证、已满足验收”

**为什么会失败**

final-answer 不是新产出的地方，而是对前面已完成事实的交付摘要。

**正确示范**

错误流程：

1. 口头说“报告已写入”
2. 直接说“满足验收标准”
3. 没有文件证据，也没有验证记录

正确流程：

1. 实际写入文件
2. 对文件存在性与大小做验证
3. 将验证结果写入 `complete_step`
4. 再用 `todo_write` 同步状态
5. 最后 final-answer 才能引用这些结果

正确范式：

- 先写文件
- 再验证文件存在且非空
- 将验证命令或结果写入 `complete_step`
- 更新 `todo_write`
- 最后做 `final-answer`

---

## 5. 最小可执行收尾顺序（建议固定套用）

任何任务结束后，尽量按下面这个最小固定顺序收尾：

1. 完成实质工作。
2. 对每条已完成任务执行一次 `complete_step`。
3. 执行一次完整的 `todo_write`，同步所有任务项状态。
4. 只有当前两步都完成，再输出 `final-answer`。

一句话记法：

> **先签字，再同步清单，最后才对外交付。**

---

## 6. 本次任务自己的收尾示范

下面就是本次任务自己按规矩走一遍，供甲方直接照抄。

### 6.1 第一步：完整提交任务清单

本次任务清单应包含：

1. 撰写《收尾规范-治本版》报告
2. 验证报告存在、非空且满足验收标准
3. 按 `complete_step → todo_write → final-answer` 顺序完成收尾

### 6.2 第二步：逐条执行 complete_step

**对第 1 条任务的正确签收应包含：**

- `summary`：已写出标准三段、任务书标准收尾段模板、3 个踩坑场景与正确示范、以及本次任务自己的收尾示范
- `paths`：`<工作目录>/
- `kind`：`manual` 或 `files`

**对第 2 条任务的正确签收应包含：**

- `summary`：已验证报告文件存在、非空，并检查包含验收要求的三大部分
- `paths`：`<工作目录>/
- `kind`：`manual` 或 `verification`

**对第 3 条任务的正确签收应包含：**

- `summary`：已按正确顺序完成签收、清单更新和最终交付准备
- `kind`：`manual`

### 6.3 第三步：执行完整 todo_write

在签收完成后，`todo_write` 应把所有实质任务同步到最终状态，而不是只更新一条。

### 6.4 第四步：最后再给 final-answer

final-answer 中只能说下面这些已落地事实：

- 报告文件路径为 `<工作目录>/
- 报告已包含：
  - 标准收尾三段
  - 任务书标准收尾段模板
  - 3 个踩坑场景 + 正确示范
  - 本次任务自己的收尾示范
- 收尾顺序遵循：`complete_step → todo_write → final-answer`

### 6.5 本次任务标准交付范式总结

按规矩收尾时，最终对外交付应遵循这个固定口径：

> 报告已写入磁盘；每条实质任务均已完成签收；任务清单已同步更新；final-answer 仅复述已签收且已落盘的事实。

这也就是本规范最终要求落地的标准姿势。
